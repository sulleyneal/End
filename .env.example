# Neon Postgres. Pooled connection for request-path queries.
DATABASE_URL=

# Direct (unpooled) connection. Used for migrations.
DATABASE_URL_UNPOOLED=

# Anthropic API key for the AI DM. Server-side only — never exposed to the client.
ANTHROPIC_API_KEY=
